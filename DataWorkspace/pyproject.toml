[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dataworkspace"          # import path will be `dataworkspace`
version = "0.1.0"
description = "Code for the TGNNs DataWorkspace"
authors = [{ name = "Your Name" }]
requires-python = ">=3.10"

[tool.setuptools.packages.find]

include = ["dataworkspace*"]